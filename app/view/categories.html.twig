<div class="widget categories">
    <header>
        <p class="categoryTitle">Catégories</p>
    </header>
    {% for category in categories %}

        <div class="item d-flex justify-content-between"><a
                    href="/P5-blog/posts/{{ category.categoryName|replace({' ': '_'}) }}/1">{{ category.categoryName }}</a><span>{{ category.nbPosts }}</span>
        </div>

    {% endfor %}
</div>
